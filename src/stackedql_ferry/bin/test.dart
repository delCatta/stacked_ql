import 'package:stackedql_ferry/client.dart';

void main(List<String> arguments) async {
  // final apiReq =
  // String token =
  // String url = "/graphql";
  // final client = await initClient(url,
  //     defaultHeaders: {
  //       "Content-Type": "application/json",
  //     },
  //     updateToken: () => Future.value(token),
  //     onException: (c) {
  //       print("HTTP LINK MENT CLIENT EXCEPTION");
  //     });
  // client.request(apiReq).listen((response) {
  //   print(response.data);
  // });
}

// Useful commands:
// dart pub run build_runner build
